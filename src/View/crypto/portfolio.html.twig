<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="/css/tailwind.css" rel="stylesheet">
    <title>Portfolio</title>
</head>
<body>
<h1 class="text-2xl font-bold">Portfolio of {{ user.name }}</h1>
<p>Wallet Balance: ${{ user.wallet.balance }}</p>
<h2 class="text-xl font-bold">Transactions</h2>
<ul>
    {% for transaction in transactions %}
        <li>{{ transaction.date }} - {{ transaction.transaction_type }} {{ transaction.amount }} {{ transaction.asset }} at ${{ transaction.price }} each. Total: ${{ transaction.total }}</li>
    {% endfor %}
</ul>
</body>
</html>
